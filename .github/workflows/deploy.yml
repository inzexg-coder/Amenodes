name: Deploy Amenodes

on:
  push:
    branches:
      - main
      - master
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

env:
  OWNER_USERNAME: "inzexg-coder"

jobs:
  check-workflow-files:
    runs-on: ubuntu-latest
    outputs:
      workflow_changed: ${{ steps.check.outputs.workflow_changed }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
      
      - name: Check for new or modified workflow files
        id: check
        run: |
          AUTHOR="${{ github.actor }}"
          OWNER="$OWNER_USERNAME"
          
          echo "Author: $AUTHOR"
          echo "Owner: $OWNER"
          
          if [ "$AUTHOR" = "$OWNER" ]; then
            echo "workflow_changed=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD 2>/dev/null || echo "")
          else
            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
          fi
          
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            NEW_FILES=$(git ls-files --others --exclude-standard | grep -E '\.github/workflows/.*\.ya?ml' || echo "")
          else
            NEW_FILES=$(git ls-files --others --exclude-standard | head -20 || echo "")
          fi
          
          echo "Changed files: $CHANGED_FILES"
          echo "New files: $NEW_FILES"
          
          if echo "$CHANGED_FILES" | grep -qE '\.github/workflows/|\.github/CODEOWNERS'; then
            echo "workflow_changed=true" >> $GITHUB_OUTPUT
            echo "::error::User $AUTHOR attempted to MODIFY protected files"
            exit 1
          fi
          
          if echo "$NEW_FILES" | grep -qE '\.github/workflows/.*\.ya?ml'; then
            echo "workflow_changed=true" >> $GITHUB_OUTPUT
            echo "::error::User $AUTHOR attempted to ADD new workflow file"
            echo "::error::New files: $NEW_FILES"
            exit 1
          fi
          
          WORKFLOW_COUNT=$(find .github/workflows -name "*.yml" 2>/dev/null | wc -l)
          if [ -d ".github/workflows" ] && [ "$WORKFLOW_COUNT" -gt 1 ]; then
            if [ -z "$(git ls-tree origin/${{ github.base_ref }} .github/workflows/ 2>/dev/null || echo "")" ]; then
              echo "workflow_changed=true" >> $GITHUB_OUTPUT
              echo "::error::User $AUTHOR attempted to CREATE workflows directory"
              exit 1
            fi
          fi
          
          if [ -f ".github/workflows/deploy.yml" ]; then
            DEPLOY_HASH=$(sha256sum .github/workflows/deploy.yml | cut -d' ' -f1)
            MAIN_HASH=$(git show origin/main:.github/workflows/deploy.yml 2>/dev/null | sha256sum | cut -d' ' -f1)
            
            if [ -n "$MAIN_HASH" ] && [ "$DEPLOY_HASH" != "$MAIN_HASH" ]; then
              echo "workflow_changed=true" >> $GITHUB_OUTPUT
              echo "::error::deploy.yml has been modified"
              exit 1
            fi
          fi
          
          echo "workflow_changed=false" >> $GITHUB_OUTPUT

  security-check:
    runs-on: ubuntu-latest
    needs: check-workflow-files
    if: needs.check-workflow-files.outputs.workflow_changed != 'true'
    outputs:
      is_allowed: ${{ steps.check.outputs.is_allowed }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      
      - name: Check author permissions
        id: check
        run: |
          AUTHOR="${{ github.actor }}"
          if [ "$AUTHOR" = "$OWNER_USERNAME" ]; then
            echo "is_allowed=true" >> $GITHUB_OUTPUT
          else
            echo "is_allowed=false" >> $GITHUB_OUTPUT
            echo "::error::User $AUTHOR is not allowed"
            exit 1
          fi

  validate:
    runs-on: ubuntu-latest
    needs: [check-workflow-files, security-check]
    if: |
      needs.check-workflow-files.outputs.workflow_changed != 'true' &&
      needs.security-check.outputs.is_allowed == 'true'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      
      - name: Check JS syntax
        run: |
          for file in $(find . -name "*.js" -type f -not -path "./.git/*" -not -path "./node_modules/*"); do
            node -c "$file" || exit 1
          done
      
      - name: Check HTML
        run: |
          if [ -f "amenodes.html" ]; then
            grep -q "<!DOCTYPE html>" amenodes.html || exit 1
          else
            exit 1
          fi

  preview:
    runs-on: ubuntu-latest
    needs: [check-workflow-files, security-check, validate]
    if: |
      needs.check-workflow-files.outputs.workflow_changed != 'true' &&
      needs.security-check.outputs.is_allowed == 'true' &&
      github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      
      - name: Get branch name
        id: branch
        run: echo "name=$(echo '${{ github.head_ref }}' | sed 's/\//-/g')" >> $GITHUB_OUTPUT
      
      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp
      
      - name: Deploy preview
        run: |
          lftp -c "
            set sftp:auto-confirm yes
            open -u ${{ secrets.SFTP_USERNAME }},${{ secrets.SFTP_PASSWORD }} sftp://${{ secrets.SFTP_HOST }}
            mkdir -p /var/www/html/preview/${{ steps.branch.outputs.name }}
            mirror -R --delete \
              --exclude='.git' \
              --exclude='.github' \
              --exclude='preview' \
              --exclude='backups' \
              ./ /var/www/html/preview/${{ steps.branch.outputs.name }}/
          "
      
      - name: Set preview URL
        id: url
        run: |
          URL="https://${{ secrets.SFTP_HOST }}/preview/${{ steps.branch.outputs.name }}/amenodes.html"
          echo "url=$URL" >> $GITHUB_OUTPUT
      
      - name: Comment PR with preview link
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const url = '${{ steps.url.outputs.url }}';
            const body = `## Preview Deployment\n\nPreview available at: ${url}\n\nBranch: ${{ github.head_ref }}\n\nCommit: ${{ github.sha }}`;
            
            const { data: comments } = await github.rest.issues.listComments({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            
            const existingComment = comments.find(c => 
              c.body && c.body.includes('Preview Deployment')
            );
            
            if (existingComment) {
              await github.rest.issues.updateComment({
                comment_id: existingComment.id,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: body
              });
            }

  production:
    runs-on: ubuntu-latest
    needs: [check-workflow-files, security-check, validate]
    if: |
      needs.check-workflow-files.outputs.workflow_changed != 'true' &&
      needs.security-check.outputs.is_allowed == 'true' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      
      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp
      
      - name: Deploy production
        run: |
          lftp -c "
            set sftp:auto-confirm yes
            open -u ${{ secrets.SFTP_USERNAME }},${{ secrets.SFTP_PASSWORD }} sftp://${{ secrets.SFTP_HOST }}
            mirror -R --delete \
              --exclude='.git' \
              --exclude='.github' \
              --exclude='preview' \
              --exclude='backups' \
              ./ /var/www/html/
          "
      
      - name: Output production URL
        run: |
          echo "Production deployed to: https://${{ secrets.SFTP_HOST }}/amenodes.html"
